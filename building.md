ビルド
---

[toolchain][1]をインストールしたらMimikerをコンパイルする準備ができました。
プロジェクトのルートディレクトリで`make`を実行する前に`config.mk`ファイルを
見てください。そこには次のような重要な設定オプションがあります。

* `KASAN=1`: Kernel Address Sanitizer（動的メモリエラー検出器）を有効に
  します。これはバッファオーバーランやその他のメモリ関連の厄介なバグを検出
  します。
* `KCSAN=1`: Kernel Concurrency Sanitizer（データ競合検出）を有効にします。
  これは数のスレッドから適切な同期なしにアクセスされる変数を特定します、
* `LOCKDEP=1`: Kernel Lock Dependency Checkerを有効にします。これは
  カーネルでデッドロックにつながる可能性のあるロックの順序違反を特定します、
* `KGPROF=1`: カーネルプロファイリングを有効にします。これはカーネルの各関数で
  費やされる時間を追跡します。
* `LLVM`を0に設定するとLLVMツールチェーン（clangとlld）の代わりにGNUツール
  チェーン（gccとbinutils）がプロジェクトのコンパイルに使用されます。

ビルドシステムをデバッグする必要がある場合、すなわち、`make`により実行される
コマンドを表示する必要がある場合は、コマンドラインに`VERBOSE=1`を追加して
ください。

**重要** `config.mk`ファイルを変更した場合は都度`make distclean`コマンドを
実行することを推奨します。

**重要** Mimkerを別のボード用にビルドする場合は、その前に以前のボードを
`BOARDP引数にセットして`make distclean`を実行必要があります。

[1]: toolchain.md
